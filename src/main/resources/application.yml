spring:
  application:
    name: tg-ecoil
  datasource:
    url: jdbc:postgresql://localhost:5432/ecoil
    username: postgres
    password: postgres
    jpa:
      hibernate:
        ddl-auto: update
        show-sql: true
